turn_on:
  service: automation.turn_off
  data:
    entity_id:
      - automation.climate_start_heating_when_cold
      - automation.climate_start_cooling_when_warm
turn_off:
  - service: automation.turn_on
    data:
      entity_id:
        - automation.climate_start_heating_when_cold
        - automation.climate_start_cooling_when_warm
platform: template 
sensors:
  electricity_price:
    unit_of_measurement: 'SEK/kWh'
    friendly_name: Electricity Price
    value_template: >-
      {{ (float(state_attr('sensor.nordpool_kwh_se4_sek', 'average')) + float(states.sensor.kraftringen_transfer_price.state) + float(states.sensor.kraftringen_energy_tax.state)) }}